{
  "name": "worldwise-server",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start-server": "json-server --watch data/cities.json --port 8080 --delay 500"
  },
  "dependencies": {
    "json-server": "0.17.3"
  }
}
